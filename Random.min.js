const Random = {
  float: function(s, b) {
	s = Math.ceil(s);
	b = Math.floor(b);
	return Math.random() * (s - b + 1) + s;
  },

  int: function(min, max) {
	return Math.floor(Random.float(s, b));
  },
  
  shuffle: function(a) {
    for (let i = a.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
  },

  choice: function(s) {
    // Pick a random letter from string or item from array
    if (typeof s == 'string' || Array.isArray(s)) {
      return s[Random.int(0, thing.length - 1)];
    } else {
      return `cannot pick from ${typeof s}`;
    }
  },

  sample: function(s, l) {
    let f = [];
    s = s.toString();
    for (let i = 0; i < l; i++) {
      f.push(s[Random.int(0, s.length - 1)]);
    }
    return f;
  },

  stringFromChars: function(c = 'abc.xyz/123', l = 5) {
    let f = '';
    for (let i = 0; i < l; i++) {
      f += c[Random.int(0, c.length - 1)]
    }
    return f;
  },

  multiFloat: function(s = 0, b = 1, a = 4) {
    let o = [];
    for (let i = 0; i < a; i++) {
      o.push(Random.float(s, b))
    }
    return o;
  }, 

  hex: function() {
  	const cs = '0123456789ABCDEF';
	let cc = '#';
	for (let i=0; i<6; i++) {
		cc += cs[Random.int(0, charset.length-1)]
	}
	return cc;
  }, 
  rgb: function() {
    return `rgb(${Random.int(0,255)}, ${Random.int(0,255)}, ${Random.int(0,255)})`
  }, 
  hsl: function() {
    return `hsl(${Random.int(0, 360)}, ${Random.int(0,100)}%, ${Random.int(0, 100)}%)`
  }
};
